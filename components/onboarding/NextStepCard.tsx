// Arquivo descontinuado — o card de "próximo passo" fazia sentido no
// onboarding com formulários, que saiu da página /progresso/<token>
// (agora um dashboard só de leitura — ver
// app/progresso/[token]/page.tsx). Este arquivo não é mais importado
// por nada; pode ser removido com
// `git rm components/onboarding/NextStepCard.tsx`.
export {};
