// Arquivo descontinuado — a barra de "X de 5 etapas concluídas" era do
// onboarding com formulários, que saiu da página /progresso/<token>
// (agora um dashboard só de leitura, com os cards de
// ProjectProgressPanel — ver app/progresso/[token]/page.tsx). Este
// arquivo não é mais importado por nada; pode ser removido com
// `git rm components/onboarding/OnboardingProgress.tsx`.
export {};
