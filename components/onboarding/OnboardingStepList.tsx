// Arquivo descontinuado — a lista de etapas do onboarding saiu da
// página /progresso/<token>, que virou um dashboard só de leitura (ver
// app/progresso/[token]/page.tsx). Este arquivo não é mais importado
// por nada; pode ser removido com
// `git rm components/onboarding/OnboardingStepList.tsx`.
export {};
