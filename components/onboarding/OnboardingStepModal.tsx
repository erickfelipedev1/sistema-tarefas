// Arquivo descontinuado — a página /progresso/<token> deixou de pedir
// pro cliente preencher formulários (informações da empresa, objetivos
// etc.) e virou um dashboard só de leitura (ver
// app/progresso/[token]/page.tsx). Este arquivo não é mais importado
// por nada; pode ser removido com
// `git rm components/onboarding/OnboardingStepModal.tsx`.
export {};
