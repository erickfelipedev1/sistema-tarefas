// Arquivo descontinuado — a página /progresso/<token> deixou de ser um
// assistente com formulários (o cliente preenchendo dados) e virou um
// dashboard só de leitura (ver app/progresso/[token]/page.tsx). Este
// arquivo não é mais importado por nada; pode ser removido com
// `git rm components/onboarding/ClientOnboarding.tsx`.
export {};
