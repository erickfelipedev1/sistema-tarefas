// Arquivo descontinuado — os metadados das 5 etapas do onboarding
// (informações da empresa, objetivos, escopo, participantes, aprovação)
// não são mais usados: a página /progresso/<token> virou um dashboard
// só de leitura (ver app/progresso/[token]/page.tsx). Este arquivo não
// é mais importado por nada; pode ser removido com
// `git rm lib/onboarding.ts`.
export {};
